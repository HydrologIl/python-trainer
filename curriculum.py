GENERAL_CURRICULUM = """
Верхнеуровневая программа обучения Python для анализа данных — от нуля
до уверенного уровня Senior Data Analyst.

Этап 0. Подготовка.
Цель: научиться понимать синтаксис Python, уверенно работать с переменными,
условиями, циклами, функциями, базовыми структурами данных и простыми файлами.

Этап 1. Основы анализа данных / Junior level.
Цель: научиться обрабатывать таблицы с помощью pandas, делать базовую
агрегацию, очистку и визуализацию.

Этап 2. Углубление и расширение / Middle level.
Цель: строить полные пайплайны анализа, уверенно использовать numpy,
настраивать графики, работать с datetime и сложными трансформациями.

Этап 3. Продвинутый анализ и автоматизация / Upper-Middle.
Цель: автоматизировать отчёты, применять сложные трансформации,
работать с API и внешними источниками.

Этап 4. Senior-уровень: системный и стратегический анализ.
Цель: мыслить архитектурно, собирать сложные пайплайны, готовить данные для ML,
документировать проекты и обучать младших.
"""


STAGE_0_CURRICULUM = """
Этап 0: Подробный план обучения с нуля, версия 2.

Блок 1. Запуск, print(), комментарии, первые ошибки.
Цель: запустить Python и научиться писать первые строки кода.
Темы: print(), комментарии #, строки в кавычках, числа без кавычек,
простые арифметические выражения, выполнение кода сверху вниз.

Блок 2. Переменные и базовые типы данных.
Цель: хранить значения в переменных и понимать базовые типы.
Темы: переменные, присваивание =, int, float, str, bool,
type(), int(), float(), str(), bool(), f-строки, простые операции.

Блок 3. Ввод, преобразование типов, условия if/elif/else.
Цель: получать ввод пользователя и управлять ветвлением программы.
Темы: input(), int(input()), float(input()), if, elif, else, отступы,
==, !=, <, >, >=, <=, and, or, not.

Блок 4. Цикл for и range().
Цель: повторять действия фиксированное количество раз.
Темы: for, range(stop), range(start, stop), range(start, stop, step),
переменная цикла, счётчик, накопитель, условие внутри цикла.

Блок 5. Цикл while, флаги, break, continue.
Цель: повторять действия до выполнения условия и контролировать остановку цикла.
Темы: while, флаг, while True, break, continue, риск бесконечного цикла.

Блок 6. Функции.
Цель: выносить логику в функции, передавать аргументы и возвращать результат.
Темы: def, имя функции, параметры, аргументы, вызов функции, return,
функция без return, локальные переменные, return vs print, несколько return.

Блок 7. Строки: базовая обработка текста.
Цель: читать, очищать, проверять и преобразовывать текст.
Темы: строка как последовательность символов, индексация, срезы, len(), in,
f-строки, .lower(), .upper(), .strip(), .replace(), .split(), .join(),
.startswith(), .endswith(), .count(), .find().

Блок 8. Списки: хранение и изменение наборов значений.
Цель: хранить наборы значений, менять список и проходить по нему циклом.
Темы: создание списка, индексация, срезы, изменение элемента по индексу,
len(), in, append(), remove(), pop(), цикл for по списку,
sum(), min(), max(), sorted().

Блок 9. Списки + функции + циклы.
Цель: писать функции, которые принимают список и возвращают результат.
Темы: функция принимает список, возвращает число/строку/список/bool,
создание нового списка через обычный for, фильтрация, подсчёт, поиск,
накопление результата.

Блок 10. Словари.
Цель: хранить структурированные данные в формате ключ-значение.
Темы: dict, ключ, значение, создание словаря, доступ по ключу,
добавление пары, изменение значения, in для проверки ключа, .get(),
.keys(), .values(), .items(), цикл по словарю.

Блок 11. Список словарей: мини-таблицы без pandas.
Цель: научиться мыслить данными как строками таблицы до изучения pandas.
Темы: список словарей, строка таблицы как dict, фильтрация записей,
сумма по условию, подсчёт по условию, поиск записей, простая ручная группировка
через словарь-накопитель.

Блок 12. Множества.
Цель: получать уникальные значения и проверять принадлежность.
Темы: set, уникальные элементы, set(list), in, add(), remove(), len(),
intersection, union, difference.

Блок 13. Файлы: txt и простой csv как текст.
Цель: читать и записывать простые файлы.
Темы: open(), with open(...) as file, read(), readlines(), write(),
режимы "r", "w", "a", encoding="utf-8", strip() при чтении строк,
split(",") для простого csv как текста.

Блок 14. Итоговый проект: анализ списка заказов.
Цель: собрать знания этапа 0 в один прикладной проект.
Темы: список словарей, функции, циклы, условия, суммы, подсчёты,
уникальные клиенты, фильтрация заказов, простые бизнес-показатели.
"""


STAGE_0_KNOWLEDGE_MATRIX = """
Матрица знаний этапа 0. Используй её как главный источник ограничений при генерации задач.

ОБЩЕЕ ПРАВИЛО:
Генерируй задачи только на обязательное ядро текущего блока и уже пройденных блоков.
Не используй конструкции из раздела “пока не трогать”.
Если конструкция не указана в текущем или предыдущих блоках, не требуй её для решения.

Блок 1. Запуск, print(), комментарии.
Обязательное ядро:
- print()
- комментарии через #
- выполнение строк сверху вниз
- строки в обычных кавычках
- числа без кавычек
- простые арифметические выражения
Частотные конструкции:
- print("Hello")
- print(2 + 3)
- print("Name:", "Anna")
Типовые ошибки:
- незакрытые кавычки
- русские кавычки “ ”
- незакрытая скобка
- текст без кавычек
Пока не трогать:
- import
- pip
- функции
- классы

Блок 2. Переменные и типы.
Обязательное ядро:
- переменная
- присваивание =
- int, float, str, bool
- type()
- int(), float(), str(), bool()
- f-строки
- простые арифметические операции
Частотные конструкции:
- name = "Anna"
- age = 30
- total = price * quantity
- message = f"{name}: {age}"
Типовые ошибки:
- строка вместо числа
- сложение строки и числа
- использование переменной до присваивания
- = вместо ==
Пока не трогать:
- None как отдельную тему
- complex
- type hints
- dataclass

Блок 3. Ввод и условия.
Обязательное ядро:
- input()
- int(input()), float(input())
- if, elif, else
- отступы
- ==, !=, <, >, >=, <=
- and, or, not
Частотные конструкции:
- if age >= 18:
- if status == "paid":
- if amount > 1000 and status == "paid":
Типовые ошибки:
- забыли преобразовать input()
- = вместо ==
- неправильные отступы
- else с условием
- путаница and/or
Пока не трогать:
- match/case
- тернарный оператор
- try/except

Блок 4. for и range().
Обязательное ядро:
- for
- range(stop)
- range(start, stop)
- range(start, stop, step)
- переменная цикла
- накопитель суммы
- счётчик
- условие внутри цикла
Частотные конструкции:
- for i in range(5):
- total = 0; total += i
- count = 0; count += 1
- if i % 2 == 0:
Типовые ошибки:
- ожидание, что range включает правую границу
- total создаётся внутри цикла
- print(total) стоит в неправильном отступе
Пока не трогать:
- enumerate
- zip
- list comprehension
- вложенные циклы

Блок 5. while, флаги, break, continue.
Обязательное ядро:
- while
- условие продолжения
- изменение переменной внутри цикла
- флаг
- while True
- break
- continue
Частотные конструкции:
- while password != "1234":
- while True: ... break
- if command == "exit": break
Типовые ошибки:
- бесконечный цикл
- переменная условия не меняется
- continue пропускает важное обновление
Пока не трогать:
- сложные state machine
- try/except для валидации ввода

Блок 6. Функции.
Обязательное ядро:
- def
- имя функции
- параметры
- аргументы
- вызов функции
- return
- функция без return
- локальная переменная
- return vs print
- несколько return в условиях
Частотные конструкции:
- def is_even(number):
- return number % 2 == 0
- result = function_name(value)
Типовые ошибки:
- функция написана, но не вызвана
- print вместо return
- результат функции не сохранён
- локальная переменная используется снаружи
- return отсутствует, функция возвращает None
Пока не трогать:
- *args, **kwargs
- lambda
- decorators
- recursion
- type hints

Блок 7. Строки.
Обязательное ядро:
- строка как последовательность символов
- индексация
- срезы
- len()
- in для подстроки
- f-строки
- .lower()
- .upper()
- .strip()
- .replace()
- .split()
- .join()
Дополнительное частотное:
- .startswith()
- .endswith()
- .count()
- .find()
Типовые ошибки:
- text.upper() не меняет text на месте
- путаница split и join
- индекс вне диапазона
- неверные границы среза
Пока не трогать:
- regex
- encoding
- translate
- сложное форматирование

Блок 8. Списки.
Обязательное ядро:
- создание списка
- индексация
- срезы
- изменение элемента по индексу
- len()
- in
- append()
- remove()
- pop()
- цикл for по списку
- sum()
- min()
- max()
- sorted()
Дополнительное:
- extend()
- enumerate() как preview, но не как обязательное решение
Типовые ошибки:
- индекс вне диапазона
- путаница remove и pop
- ожидание, что append() возвращает новый список
- изменение списка во время цикла
- путаница sorted() и .sort()
Пока не трогать:
- list comprehension как основной инструмент
- zip
- map/filter
- сложные вложенные списки

Блок 9. Списки + функции + циклы.
Обязательное ядро:
- функция принимает список
- функция возвращает число, строку, bool или новый список
- создание нового списка через обычный for
- фильтрация списка
- подсчёт элементов по условию
- поиск элемента
- накопление суммы
Частотные конструкции:
- def get_even(numbers):
- result = []; result.append(item)
- if item > limit:
- return result
Типовые ошибки:
- return внутри цикла слишком рано
- забыли создать result до цикла
- забыли append
- функция меняет исходный список, когда нужно вернуть новый
Пока не трогать:
- list comprehension как обязательный способ
- lambda
- sorted с key

Блок 10. Словари.
Обязательное ядро:
- dict
- ключ
- значение
- создание словаря
- доступ по ключу
- добавление пары
- изменение значения
- in для проверки ключа
- .get()
- .keys()
- .values()
- .items()
- цикл по словарю
Частотные конструкции:
- person["name"]
- person.get("salary", 0)
- person["city"] = "Yerevan"
- for key, value in person.items():
Типовые ошибки:
- KeyError
- путаница ключа и значения
- in проверяет ключи, а не значения
- неправильный цикл по словарю
Пока не трогать:
- defaultdict
- Counter
- dict comprehension
- JSON
- глубоко вложенные dict

Блок 11. Список словарей.
Обязательное ядро:
- список словарей
- словарь как строка таблицы
- for order in orders
- order["amount"]
- фильтрация записей по условию
- сумма по условию
- подсчёт по условию
- список уникальных значений через обычный список или set, если блок 12 уже пройден
- ручная группировка через dict-накопитель
Частотные конструкции:
- for order in orders:
- if order["status"] == "paid":
- total += order["amount"]
- result.append(order)
- totals[client] = totals.get(client, 0) + amount
Типовые ошибки:
- обращение к orders["amount"] вместо order["amount"]
- путаница списка и словаря
- KeyError из-за неверного имени ключа
- return внутри цикла слишком рано
Пока не трогать:
- pandas
- SQL
- groupby из pandas
- сложные вложенные структуры

Блок 12. Множества.
Обязательное ядро:
- set
- уникальные элементы
- set(list)
- in
- add()
- remove()
- len()
- intersection
- union
- difference
Частотные конструкции:
- unique_clients = set(clients)
- if client in unique_clients:
- a & b, a | b, a - b или методы intersection/union/difference
Типовые ошибки:
- ожидание сохранения порядка
- обращение по индексу
- потеря дублей, когда дубли были важны
Пока не трогать:
- frozenset
- сложная теория множеств

Блок 13. Файлы.
Обязательное ядро:
- open()
- with open(...) as file
- read()
- readlines()
- write()
- режимы "r", "w", "a"
- encoding="utf-8"
- strip() при чтении строк
- split(",") для простого csv как текста
Частотные конструкции:
- with open("file.txt", "r", encoding="utf-8") as file:
- text = file.read()
- lines = file.readlines()
- file.write(text)
Типовые ошибки:
- FileNotFoundError
- перезапись файла через "w"
- забыли encoding
- не убрали \\n
- путаница read и readlines
Пока не трогать:
- pathlib
- os
- pandas.read_csv
- json
- Excel

Блок 14. Итоговый проект.
Обязательное ядро:
- список словарей с заказами
- функции
- циклы
- условия
- суммы
- подсчёты
- фильтрация
- уникальные клиенты
- простая ручная группировка
Пока не трогать:
- pandas
- numpy
- SQL
- визуализации
- классы
"""


STAGE_0_FINAL_PROJECT_ORDERS = """
Итоговый проект этапа 0: анализ списка заказов.

Контекст:
Есть список заказов. Каждый заказ — словарь с полями:
- order_id: номер заказа
- client: имя клиента
- amount: сумма заказа
- status: статус заказа: "paid", "cancelled", "new"
- country: страна
- product: товарная категория

Пример данных:
orders = [
    {"order_id": 1, "client": "Anna", "amount": 1200, "status": "paid", "country": "Armenia", "product": "books"},
    {"order_id": 2, "client": "Oleg", "amount": 800, "status": "cancelled", "country": "Georgia", "product": "games"},
    {"order_id": 3, "client": "Maria", "amount": 2500, "status": "paid", "country": "Armenia", "product": "laptop"},
    {"order_id": 4, "client": "Anna", "amount": 700, "status": "paid", "country": "Armenia", "product": "books"},
]

Базовые функции проекта:
1. get_total_paid(orders)
   Возвращает сумму amount только по заказам со status == "paid".

2. get_clients(orders)
   Возвращает список клиентов из заказов.

3. get_unique_clients(orders)
   Возвращает уникальных клиентов.

4. get_large_orders(orders, limit)
   Возвращает список заказов, где amount > limit.

5. get_orders_by_status(orders, status)
   Возвращает список заказов с нужным статусом.

6. get_client_total(orders, client)
   Возвращает сумму оплаченных заказов конкретного клиента.

7. get_country_totals(orders)
   Возвращает словарь: страна -> сумма оплаченных заказов.

8. get_product_counts(orders)
   Возвращает словарь: категория товара -> количество заказов.

Критерий готовности:
Студент может написать эти функции без pandas, используя только списки,
словари, циклы, условия, функции, return и базовые операции с set.
"""
